# dev venv used for off-reactor testing
.venv-dev/
# build artifacts
dist/
build/
*.egg-info/
# python
__pycache__/
*.pyc
.pytest_cache/

# local one-time migration (contains internal infra names — do not publish)
migrate_postgres_to_lake.py
